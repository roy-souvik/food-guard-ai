# Only ROCM-specific PyTorch packages (install with: pip install -r requirements-rocm.txt --extra-index-url https://download.pytorch.org/whl/rocm6.4)
# This file should be used alongside the main requirements.txt which contains all other packages.
# Example installation:
#   pip install -r requirements.txt
#   pip install -r requirements-rocm.txt --extra-index-url https://download.pytorch.org/whl/rocm6.4

torch>=2.0
torchvision>=0.15.0
torchaudio>=2.0
